spring:
  config:
    activate:
      on-profile: local, test
  datasource:
    url: jdbc:mysql://localhost:3306/concertreservation?characterEncoding=UTF-8&serverTimezone=UTC
    username: application
    password: application

  # Redis
  redis:
    host: localhost
    port: 6379